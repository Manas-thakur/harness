# Local AI Research & Study Agent Dependencies
# Core CLI and UI
typer>=0.9.0
rich>=13.0.0
# Claude-Code-style pinned input box, history, and slash autocomplete
prompt_toolkit>=3.0.0

# LLM Client (Local Ollama) — >=0.3 for native tool/function calling
ollama>=0.3.0

# Web Search (Free, No API Key)
# Package was renamed duckduckgo-search -> ddgs; code prefers ddgs, falls back.
ddgs>=6.0.0
# Page fetching for the fetch_url tool (read full pages, not just snippets).
httpx>=0.27.0
lxml>=4.9.0

# PDF Reading
pypdf>=3.0.0

# YAML Parsing for Skills
pyyaml>=6.0

# Testing
pytest>=7.0.0
